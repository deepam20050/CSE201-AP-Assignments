package ladder;
import common.Common;

public class Ladder extends Common {
  public Ladder () {
    super();
    this.update_empty(false);
    this.update_floor(12);
    this.update_floor_name("Ladder");
    this.update_points(2);
  }
}
